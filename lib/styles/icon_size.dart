enum WAIconSize {
  /// The size is 16.
  xxs(16),

  /// The size is 20.
  xs(20),

  /// The size is 26.
  sm(26),

  /// The size is 32.
  md(32),

  /// The size is 48.
  lg(48),

  /// The size is 60.
  xl(60);

  const WAIconSize(this.size);

  final double size;
}
